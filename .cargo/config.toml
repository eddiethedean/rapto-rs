[build]
# Enable architecture-specific optimizations for Apple Silicon
# This allows the compiler to use all available CPU features
rustflags = ["-C", "target-cpu=native"]

[target.aarch64-apple-darwin]
# Enable native CPU optimizations for aarch64 (Apple Silicon)
rustflags = ["-C", "target-cpu=native"]
